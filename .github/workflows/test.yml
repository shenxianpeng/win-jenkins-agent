name: Test

on:
  push:
    branches: main
  workflow_dispatch:

jobs: 
  test:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2

    - name: install test
      run: 
        ./main.bat all
